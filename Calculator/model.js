var calcOperations = {
    add(firstNum, secondNum){
        return parseInt(firstNum)+ parseInt(secondNum);
    },
    subtract(firstNum, secondNum){
       return parseInt(firstNum) - parseInt(secondNum);
}
}
